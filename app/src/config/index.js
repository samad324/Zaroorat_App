import { APP_ID } from "./facebook.config";
import { firebaseConfig, db, initFirebase } from "./firebase.config";

/**
|--------------------------------------------------
| Main Configurations Hub 🍔
|--------------------------------------------------
*/

export { APP_ID, firebaseConfig, db, initFirebase };
